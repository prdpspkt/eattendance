# Django Core
django==5.2.12
djangorestframework==3.17.1
# Required by ImageField (User.profile_picture)
pillow==12.3.0

# Celery for Background Tasks
celery==5.6.2
redis==7.4.0
django-celery-beat==2.9.0

# Pull-mode device communication. This library speaks the ZK protocol on TCP
# 4370 and is therefore vendor-specific; push mode (ADMS/WDMS) needs no
# library and works with any terminal supporting it.
pyzk==0.9

# Reporting
openpyxl==3.1.5
reportlab==4.4.10

# WSGI server. Configured by deploy/gunicorn.conf.py; the default
# invocation runs a single worker, which is not what this box wants.
gunicorn==23.0.0

# Static file serving (works without nginx in front)
whitenoise==6.12.0

# Utilities
python-decouple==3.8
